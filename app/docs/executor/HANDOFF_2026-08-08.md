# KR1688 Project Handoff — 2026-08-08

## Project identity

- Product: Saudi-first, Arabic-native Chinese story publishing platform.
- V1 scope: RTL reading; Story, Chapter and Media content; reader accounts; favorites; reading progress; comments with moderation; internal AI task layer.
- Explicitly excluded: author uploads/revenue sharing, payment, Meilisearch, recommendation engine, native app, public deployment, sitemap, RSS, JSON-LD and IndexNow.
- Repository: `https://github.com/xhs13689060096-a11y/kr1688`
- Local checkout: `/Users/xiaotudou/Documents/Codex/kr1688`
- Active branch: `marvis/ops-01-automated-acceptance`
- Base branch: `main` — do not alter or merge without Codex acceptance.

## Single source of execution truth

1. `app/docs/executor/STATUS.yaml` is the only live task state.
2. `app/docs/executor/KR1688_CURRENT_EXECUTOR_PACKET.md` is the only current Marvis task contract.
3. GitHub Actions for the exact branch head is the only final acceptance evidence.
4. Earlier Phase 2B reports and Markdown status files are historical evidence, not live instructions.

## Current state at handoff

- Packet: `KR1688-EXECUTION-01-OPS-SECURITY`.
- State: `in_progress`.
- Completed: `A01` — explicit test environment, commit `1ce9053e0d163f495717bf923c7e711424166651`.
- Current next task: `A02` — restore real lint and create the guardrail verifier.
- CI: not configured/run yet; `ci_conclusion: not_run`.
- Local PostgreSQL is unavailable. This is recorded evidence only, not a pass: the GitHub Actions PostgreSQL service in A04 must execute all required integration and E2E tests.
- Uncommitted path observed at handoff: `app/tests/fixtures/`. Treat it as Marvis A02 work-in-progress; inspect it before changing or deleting anything.

## Required execution order

`A02 → A03 → A04 → A05 → C01 → C02 → C03 → C04 → C05 → C06`

Stage A establishes truthful CI. Stage C is blocked until A05 is green on the exact head SHA. C06 is the only task allowed to request Codex acceptance. No phase may be marked accepted, merged, or deployed by Marvis.

## Approved security baseline

Phase 2C is approved and designed in:

- `app/docs/superpowers/specs/2026-08-08-phase-2c-security-baseline-design.md`
- `app/docs/superpowers/plans/2026-08-08-phase-2c-security-baseline.md`

It covers environment validation, Payload login lockout, secure account roles, API permission tests, comment moderation/ownership integrity, database-role contract, backup/restore contract, and Phase 3A Cloudflare/HTTPS/2FA launch gates.

It does not connect a real Cloudflare account, production PostgreSQL, R2, backup provider, 2FA provider, or deployment. Those are Phase 3A public-launch gates.

## Operating rules for Marvis

- Work only on the active branch and push focused commits after each successful task.
- For every behavior change, record the RED result before the smallest GREEN fix.
- A skipped, excluded, discovered-only, or service-unavailable required test is never a completed test.
- `STATUS.yaml` must name actual command results, next task, branch SHA, CI URL and CI conclusion.
- After two failed minimal attempts for one root cause, stop and report evidence.
- Never put real credentials in the repo, status files, reports, GitHub Actions, or chat.

## Latest key commits

- `2e38deb` — unified execution state issued.
- `1ce9053` — A01 explicit test environment.
- `1668974` — A01 state update, next task A02.

## New-project startup instruction

Start by reading this handoff, then `STATUS.yaml`, then the current executor packet. Continue only from the `next_task` in `STATUS.yaml`; do not reconstruct tasks from chat history.
